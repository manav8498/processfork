[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "processfork-claude-code"
version = "1.0.0"
description = "ProcessFork integration for Claude Code — adds /snapshot, /fork, /merge slash commands."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "manav8498", email = "manavpatel91570@gmail.com" }]
dependencies = [
    "processfork>=1.0.0",
]

[project.scripts]
pf-wrap-claude = "processfork_claude_code.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["processfork_claude_code"]
